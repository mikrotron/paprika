import { Story, Meta, Preview, Props } from "@storybook/addon-docs/blocks";
import MdxHeader from "storybook/assets/documentation/MdxHeader/MdxHeader";
import packageJson from "../package.json";
import Button from "../src/";
import Showcase from "./examples/Showcase";
import Basic from "./examples/Basic";

<Meta title="Button" parameters={{ options: { showPanel: false } }} />

<MdxHeader
  componentTitle="Button"
  description="The Button component is a fully accessible button intended for use typically as a skeuomorphic button, but many visual styles (kinds) are available."
  packageJson={packageJson}
/>

## Props table

<Props of={Button} />

## Stories

<Preview>
  <Story name="Example - Basic">
    <Button onClick={()=>{}}>This is my button</Button>
  </Story>
</Preview>
