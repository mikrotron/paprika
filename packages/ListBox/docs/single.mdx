---
name: single selection
route: "/listbox/single"
menu: "Listbox"
---

<!------------------- IMPORTS ------------------>

import ListBox from "../index";
import { ListBox as ListBoxProps } from "../ListBox";
import OptionProps from "../components/Option";
import GroupProps from "../components/Group";
import * as Single from "../stories/examples/single";

import { Playground, PropsTable } from "docz";

# Single selection

## Getting started

```sh
  npm install @paprika/listbox
```

or

```sh
  yarn add @paprika/listbox
```

## Basic

<Playground>
  <ListBox zIndex={10000} getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
  </ListBox>
</Playground>

```js
<ListBox>
  <ListBox.Option>Spiderman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Carnage</ListBox.Option>
</ListBox>
```

## Props

### ListBox

<PropsTable of={ListBoxProps} />

### ListBox.Option

<PropsTable of={OptionProps} />

### ListBox.Group

<PropsTable of={GroupProps} />

## Examples

### Displaying the listbox inline:

<Playground>
  <Single.BasicInlineDisplay />
</Playground>

Code:

```js
<ListBox isInline>
  <ListBox.Option>Punisher</ListBox.Option>
  <ListBox.Option>Catwoman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Thunderbolts</ListBox.Option>
  <ListBox.Option>Deadpool</ListBox.Option>
  <ListBox.Option>Spawn</ListBox.Option>
  <ListBox.Option>Wolverine</ListBox.Option>
</ListBox>
```

---

### With a filter:

<Playground>
  <Single.WithFilter />
</Playground>

Code:

```js
<ListBox hasFilter>
  <ListBox.Option>Punisher</ListBox.Option>
  <ListBox.Option>Catwoman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Thunderbolts</ListBox.Option>
  <ListBox.Option>Deadpool</ListBox.Option>
  <ListBox.Option>Spawn</ListBox.Option>
  <ListBox.Option>Wolverine</ListBox.Option>
</ListBox>
```

---

### With a custom renderTrigger:

<Playground>
  <ListBox
    renderTrigger={(state, dispatch, { getDOMAttributesForListBoxButton }) => (
      <button
        onClick={() => {
          dispatch({ type: "OPEN_POPOVER" });
        }}
        type="button"
        {...getDOMAttributesForListBoxButton()}
        ref={state.refTrigger}
      >
        <span role="img" aria-label="rocket">
          🚀
        </span>
        Toggle Listbox
      </button>
    )}
    zIndex={10000}
  >
    <ListBox.Option>Punisher</ListBox.Option>
    <ListBox.Option>Catwoman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Thunderbolts</ListBox.Option>
    <ListBox.Option>Deadpool</ListBox.Option>
    <ListBox.Option>Spawn</ListBox.Option>
    <ListBox.Option>Wolverine</ListBox.Option>
  </ListBox>
</Playground>

Code:

```js
<ListBox renderTrigger={(state, dispatch, { getDOMAttributesForListBoxButton }) => <button>🚀 Toggle Listbox</button>}>
  <ListBox.Option>Punisher</ListBox.Option>
  <ListBox.Option>Catwoman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Thunderbolts</ListBox.Option>
  <ListBox.Option>Deadpool</ListBox.Option>
  <ListBox.Option>Spawn</ListBox.Option>
  <ListBox.Option>Wolverine</ListBox.Option>
</ListBox>
```
