---
name: multiple selection
route: "/listbox/multiple"
menu: "Listbox"
---

<!------------------- IMPORTS ------------------>

import ListBox from "../index";
import { ListBox as ListBoxProps } from "../ListBox";
import OptionProps from "../components/Option";
import GroupProps from "../components/Group";
import * as Multi from "../stories/examples/multi";

import { Playground, PropsTable } from "docz";

# Multiple selection

## Getting started

```sh
  npm install @paprika/listbox
```

or

```sh
  yarn add @paprika/listbox
```

## Basic

The Multi select behaves the same way as the single select and has the same props.

To make the `<ListBox />` behave as a multi select, the developer needs to pass the _prop_ isMulti
`<ListBox isMulti />`.

> Note:
> In single select, `onChange` event returns the selected index number, while multi select will return an array of indexes.

<Playground>
  <ListBox isMulti zIndex={10000} getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
  </ListBox>
</Playground>

```js
<ListBox isMulti>
  <ListBox.Option>Spiderman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Carnage</ListBox.Option>
</ListBox>
```

## Props

### ListBox

<PropsTable of={ListBoxProps} />

### ListBox.Option

<PropsTable of={OptionProps} />

### ListBox.Group

<PropsTable of={GroupProps} />

## Examples

### Displaying ListBox with custom checkboxes:

<Playground>
  <ListBox
    isMulti
    renderCheckbox={isChecked => {
      return isChecked ? "✅" : "🙅‍";
    }}
    getScrollContainer={() => document.querySelector("#root > div")}
  >
    <ListBox.Option>Punisher</ListBox.Option>
    <ListBox.Option>Catwoman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
  </ListBox>
</Playground>

To display custom checkboxes, add `renderCheckbox` with a function returning
the type of checkbox to render depeneding on whether the user has selected
the option or not.

To display no checkboxes beside each option, return `null`. For example,
`renderCheckbox={()=>null}`.

> Note: Custom check boxes also work in single select.

Code:

```js
<ListBox
  isMulti
  renderCheckbox={isChecked => {
    return isChecked ? "✅" : "🙅‍";
  }}
>
  <ListBox.Option>Punisher</ListBox.Option>
  <ListBox.Option>Catwoman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
</ListBox>
```

---
